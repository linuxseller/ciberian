fn main() : void {
    i32 iterations=10;
    i32 a[iterations];
    std.print "For started\n";
    for(i32 i=0; i<iterations; i+=1;){
        a[i] = std.random()%1000;
        std.print "Hi " i "\n";
    }
    std.print a "\n";
}
